# 小程序端UI页面设计 - 老人健康记录小程序

## 文档信息
- **项目名称**: 老人健康记录小程序
- **文档版本**: v1.0
- **创建日期**: 2026-01-31
- **文档类型**: 小程序端UI页面设计文档

## 1. 首页(index)

### 1.1 页面布局图

```
┌─────────────────────────────────┐
│ 📍 首页                        │
├─────────────────────────────────┤
│                                 │
│  ━━━━━━━━━━━━━━━━━━━━━━━━    │ ← 异常预警横幅(如存在)
│  ⚠️ 血糖偏高,建议及时就医!    │    红色背景,可关闭
│  ━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                 │
├─────────────────────────────────┤
│    📊 今日血糖                  │
│  ┌───────────────────────────┐  │
│  │                           │  │
│  │      7.2                  │  │ ← 大数字显示(96rpx)
│  │   mmol/L                  │  │ ← 单位(32rpx)
│  │                           │  │
│  │   [空腹]  [07:30]        │  │ ← 测量类型和时间
│  │                           │  │
│  │  ━━━━━━━━━━━━━━━━━━━  ━━│  │ ← 分割线
│  │                           │  │
│  │      10.5                 │  │
│  │   mmol/L                  │  │
│  │                           │  │
│  │   [早餐后]  [09:00]       │  │
│  └───────────────────────────┘  │
│  [查看全部血糖 →]              │
├─────────────────────────────────┤
│    💓 今日血压                  │
│  ┌───────────────────────────┐  │
│  │                           │  │
│  │  145 / 95  🖤 78         │  │ ← 血压值/心率
│  │     ↑  ↑     ↑            │  │
│  │  收缩 舒张 心率          │  │
│  │   压   压  (次/分)       │  │
│  │                           │  │
│  │   [08:00]                 │  │ ← 测量时间
│  └───────────────────────────┘  │
│  [查看全部血压 →]              │
├─────────────────────────────────┤
│                                 │
│    快捷操作                     │
│  ┌─────────────┬─────────────┐  │
│  │   📷        │   📷        │  │
│  │             │             │  │
│  │  记录血糖    │  记录血压    │  │ ← 两个大按钮
│  │             │             │  │    橙色背景,圆角
│  └─────────────┴─────────────┘  │
│                                 │
├─────────────────────────────────┤
│    最近记录                     │
│                                 │
│  ┌───────────────────────────┐  │
│  │ 🩸 7.2 mmol/L          [>] │  │ ← 血糖记录卡片
│  │   空腹 07:30           │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ 💓 145/95 mmHg         [>] │  │ ← 血压记录卡片
│  │   08:00                │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ 🩸 10.5 mmol/L         [>] │  │
│  │   早餐后 09:00          │  │
│  └───────────────────────────┘  │
│                                 │
├─────────────────────────────────┤
│   [首页] [记录] [我的]         │ ← 底部导航栏
└─────────────────────────────────┘
```

### 1.2 组件设计规范

#### 异常预警横幅
```css
.alert-banner {
  background: linear-gradient(90deg, #FF6B6B, #FF8787);
  padding: 24rpx 32rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  color: #FFFFFF;
}

.alert-banner-icon {
  font-size: 48rpx;
  margin-right: 16rpx;
}

.alert-banner-text {
  flex: 1;
  font-size: 28rpx;
  font-weight: var(--font-weight-medium);
}

.alert-banner-close {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
}
```

#### 今日血糖卡片
```css
.today-card {
  background: var(--bg-primary);
  border-radius: var(--radius-lg);
  padding: 48rpx 32rpx;
  margin-bottom: 24rpx;
  box-shadow: var(--shadow-md);
}

.today-card-title {
  font-size: 32rpx;
  font-weight: var(--font-weight-semibold);
  color: var(--text-primary);
  margin-bottom: 32rpx;
  display: flex;
  align-items: center;
}

.today-card-title-icon {
  font-size: 40rpx;
  margin-right: 12rpx;
}

.glucose-record {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32rpx 0;
}

.glucose-value {
  font-size: 96rpx;
  font-family: var(--font-family-number);
  font-weight: var(--font-weight-bold);
  color: var(--primary-color);
  line-height: 1;
}

.glucose-unit {
  font-size: 32rpx;
  color: var(--text-secondary);
  margin-top: 8rpx;
}

.glucose-info {
  display: flex;
  gap: 32rpx;
  margin-top: 16rpx;
}

.glucose-tag {
  background: var(--secondary-color);
  color: var(--primary-color);
  padding: 8rpx 24rpx;
  border-radius: 100rpx;
  font-size: 28rpx;
  font-weight: var(--font-weight-medium);
}

.divider {
  height: 1rpx;
  background: var(--border-primary);
  margin: 24rpx 0;
}

.view-all-link {
  text-align: center;
  padding-top: 16rpx;
  font-size: 28rpx;
  color: var(--primary-color);
}
```

#### 快捷操作按钮
```css
.quick-actions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24rpx;
  padding: 24rpx 0;
}

.action-button {
  background: var(--primary-color);
  border-radius: var(--radius-lg);
  padding: 48rpx 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 240rpx;
  box-shadow: var(--shadow-md);
  transition: all 0.2s;
}

.action-button:active {
  transform: scale(0.98);
  background: var(--primary-hover);
}

.action-button-icon {
  font-size: 96rpx;
  margin-bottom: 16rpx;
}

.action-button-text {
  font-size: 36rpx;
  font-weight: var(--font-weight-semibold);
  color: #FFFFFF;
}
```

---

## 2. 血糖记录页面(record-glucose)

### 2.1 页面布局图

```
┌─────────────────────────────────┐
│ ← 记录血糖                      │
├─────────────────────────────────┤
│                                 │
│    血糖值                       │
│  ┌───────────────────────────┐  │
│  │                           │  │
│  │       7.2                │  │ ← 超大数字输入框
│  │                           │  │    居中显示
│  │                           │  │    橙色边框
│  └───────────────────────────┘  │
│        mmol/L                    │
│                                 │
│    测量时段                     │
│  ┌───────────────────────────┐  │
│  │ [•] 空腹                  │  │ ← 单选按钮组
│  │ [ ] 早餐后                │  │    大单选框(48rpx)
│  │ [ ] 午餐后                │  │
│  │ [ ] 晚餐后                │  │
│  │ [ ] 睡前                  │  │
│  └───────────────────────────┘  │
│                                 │
│    测量时间                     │
│  ┌───────────────────────────┐  │
│  │ 2026-01-31  07:30  [▼] │  │ ← 日期时间选择器
│  └───────────────────────────┘  │
│                                 │
│  ┌───────────────────────────┐  │
│  │  📸                     │  │
│  │                         │  │ ← 拍照识别按钮
│  │  拍照自动识别血糖值       │  │    白色背景
│  └───────────────────────────┘  │
│                                 │
│  ┌───────────────────────────┐  │
│  │  📷                     │  │
│  │                         │  │ ← 从相册选择按钮
│  │  从相册选择照片           │  │    白色背景
│  └───────────────────────────┘  │
│                                 │
│    备注(可选)                   │
│  ┌───────────────────────────┐  │
│  │ 请输入备注...            │  │ ← 多行文本输入框
│  │                          │ │
│  └───────────────────────────┘  │
│                                 │
│  ┌───────────────────────────┐  │
│  │        保存               │  │ ← 大保存按钮
│  └───────────────────────────┘  │    全宽,橙色
│                                 │
└─────────────────────────────────┘
```

### 2.2 组件设计规范

#### 大数字输入框
```css
.number-input-wrapper {
  padding: 48rpx 32rpx;
  background: var(--bg-primary);
  border-radius: var(--radius-lg);
  margin-bottom: 32rpx;
}

.number-input-label {
  font-size: 32rpx;
  font-weight: var(--font-weight-semibold);
  color: var(--text-primary);
  margin-bottom: 24rpx;
}

.large-number-input {
  height: 160rpx;
  padding: 0 48rpx;
  font-size: 96rpx;
  font-family: var(--font-family-number);
  font-weight: var(--font-weight-bold);
  text-align: center;
  border: 4rpx solid var(--primary-color);
  border-radius: var(--radius-lg);
  background: var(--bg-primary);
  color: var(--primary-color);
  box-shadow: 0 0 0 8rpx rgba(255, 159, 67, 0.1);
}

.large-number-input:focus {
  box-shadow: 0 0 0 16rpx rgba(255, 159, 67, 0.2);
}

.input-unit {
  text-align: center;
  font-size: 32rpx;
  color: var(--text-secondary);
  margin-top: 16rpx;
}
```

#### 测量时段选择器
```css
.measure-type-wrapper {
  padding: 32rpx;
  background: var(--bg-primary);
  border-radius: var(--radius-lg);
  margin-bottom: 32rpx;
}

.measure-type-label {
  font-size: 32rpx;
  font-weight: var(--font-weight-semibold);
  color: var(--text-primary);
  margin-bottom: 24rpx;
}

.measure-type-options {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.measure-type-item {
  display: flex;
  align-items: center;
  padding: 24rpx;
  background: var(--bg-secondary);
  border-radius: var(--radius-md);
  border: 2rpx solid transparent;
  transition: all 0.2s;
}

.measure-type-item.selected {
  border-color: var(--primary-color);
  background: var(--secondary-color);
}

.measure-type-radio {
  width: 48rpx;
  height: 48rpx;
  margin-right: 24rpx;
  border: 3rpx solid var(--border-secondary);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.measure-type-item.selected .measure-type-radio {
  border-color: var(--primary-color);
  background: var(--primary-color);
}

.measure-type-radio-inner {
  width: 24rpx;
  height: 24rpx;
  background: #FFFFFF;
  border-radius: 50%;
}

.measure-type-text {
  font-size: 32rpx;
  color: var(--text-primary);
  font-weight: var(--font-weight-medium);
}

.measure-type-item.selected .measure-type-text {
  color: var(--primary-color);
}
```

#### 拍照识别按钮
```css
.photo-actions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24rpx;
  margin-bottom: 32rpx;
}

.photo-button {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 48rpx 32rpx;
  background: var(--bg-primary);
  border: 2rpx solid var(--border-primary);
  border-radius: var(--radius-lg);
  transition: all 0.2s;
}

.photo-button:active {
  border-color: var(--primary-color);
  background: var(--secondary-color);
}

.photo-button-icon {
  font-size: 80rpx;
  margin-bottom: 16rpx;
}

.photo-button-text {
  font-size: 28rpx;
  color: var(--text-primary);
}
```

---

## 3. 血压记录页面(record-pressure)

### 3.1 页面布局图

```
┌─────────────────────────────────┐
│ ← 记录血压                      │
├─────────────────────────────────┤
│                                 │
│    血压值                       │
│  ┌───────────────────────────┐  │
│  │   收缩压(高压)            │  │
│  │                           │  │
│  │       145                 │  │ ← 大数字输入框
│  │                           │  │
│  └───────────────────────────┘  │
│       mmHg                      │
│                                 │
│  ┌───────────────────────────┐  │
│  │   舒张压(低压)            │  │
│  │                           │  │
│  │       95                  │  │ ← 大数字输入框
│  │                           │  │
│  └───────────────────────────┘  │
│       mmHg                      │
│                                 │
│    心率(可选)                   │
│  ┌───────────────────────────┐  │
│  │                           │  │
│  │       78                  │  │ ← 数字输入框
│  │                           │  │
│  └───────────────────────────┘  │
│      次/分                      │
│                                 │
│    测量时间                     │
│  ┌───────────────────────────┐  │
│  │ 2026-01-31  08:00  [▼] │  │ ← 日期时间选择器
│  └───────────────────────────┘  │
│                                 │
│  ┌───────────────────────────┐  │
│  │  📸                     │  │
│  │                         │  │
│  │  拍照自动识别血压值       │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │  📷                     │  │
│  │                         │  │
│  │  从相册选择照片           │  │
│  └───────────────────────────┘  │
│                                 │
│    备注(可选)                   │
│  ┌───────────────────────────┐  │
│  │ 请输入备注...            │  │
│  │                          │ │
│  └───────────────────────────┘  │
│                                 │
│  ┌───────────────────────────┐  │
│  │        保存               │  │
│  └───────────────────────────┘  │
│                                 │
└─────────────────────────────────┘
```

### 3.2 特殊组件设计

#### 血压值输入区
```css
.blood-pressure-inputs {
  padding: 32rpx;
  background: var(--bg-primary);
  border-radius: var(--radius-lg);
  margin-bottom: 32rpx;
}

.pressure-input-group {
  margin-bottom: 32rpx;
}

.pressure-input-group:last-child {
  margin-bottom: 0;
}

.pressure-label {
  font-size: 32rpx;
  font-weight: var(--font-weight-medium);
  color: var(--text-primary);
  margin-bottom: 16rpx;
  display: flex;
  align-items: center;
}

.pressure-label-icon {
  font-size: 32rpx;
  margin-right: 8rpx;
}

.pressure-input-wrapper {
  position: relative;
}

.pressure-input {
  height: 120rpx;
  padding: 0 48rpx;
  font-size: 72rpx;
  font-family: var(--font-family-number);
  font-weight: var(--font-weight-bold);
  text-align: center;
  border: 3rpx solid var(--border-primary);
  border-radius: var(--radius-lg);
  background: var(--bg-primary);
  color: var(--primary-color);
}

.pressure-input:focus {
  border-color: var(--primary-color);
  box-shadow: 0 0 0 8rpx rgba(255, 159, 67, 0.1);
}

.pressure-unit {
  text-align: center;
  font-size: 28rpx;
  color: var(--text-secondary);
  margin-top: 12rpx;
}
```

---

## 4. 历史记录页面(history)

### 4.1 页面布局图

```
┌─────────────────────────────────┐
│ ← 历史记录                      │
├─────────────────────────────────┤
│  [全部▼]  [今天]  [本周]  [本月] │ ← 日期筛选Tab
├─────────────────────────────────┤
│                                 │
│  2026年01月31日  星期五        │
│  ┌───────────────────────────┐  │
│  │ 🩸 7.2 mmol/L      [删除] │  │ ← 记录卡片
│  │   空腹 07:30  [查看详情]  │  │    可左滑删除
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ 🩸 10.5 mmol/L     [删除] │  │
│  │   早餐后 09:00  [查看详情] │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ 💓 145/95 mmHg      [删除] │  │
│  │   08:00  心率78  [查看详情]│  │
│  └───────────────────────────┘  │
│                                 │
│  2026年01月30日  星期四        │
│  ┌───────────────────────────┐  │
│  │ 🩸 6.8 mmol/L      [删除] │  │
│  │   空腹 07:15  [查看详情]  │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ 💓 138/88 mmHg     [删除] │  │
│  │   07:50  心率72  [查看详情]│  │
│  └───────────────────────────┘  │
│                                 │
│         ━━━━━━━━━━━━━         │ ← 加载更多
│         正在加载更多记录...      │
│                                 │
├─────────────────────────────────┤
│   [首页] [记录] [我的]         │
└─────────────────────────────────┘
```

### 4.2 组件设计规范

#### 日期筛选Tab
```css
.date-tabs {
  display: flex;
  background: var(--bg-primary);
  padding: 8rpx 16rpx;
  border-bottom: 1rpx solid var(--border-primary);
  overflow-x: auto;
}

.date-tab-item {
  flex-shrink: 0;
  padding: 16rpx 32rpx;
  font-size: 28rpx;
  color: var(--text-secondary);
  border-radius: var(--radius-sm);
  transition: all 0.2s;
  white-space: nowrap;
}

.date-tab-item.active {
  background: var(--primary-color);
  color: #FFFFFF;
  font-weight: var(--font-weight-medium);
}
```

#### 历史记录卡片
```css
.history-date-header {
  padding: 32rpx 32rpx 16rpx;
  font-size: 28rpx;
  font-weight: var(--font-weight-semibold);
  color: var(--text-secondary);
  background: var(--bg-secondary);
}

.history-record {
  background: var(--bg-primary);
  padding: 32rpx;
  margin-bottom: 16rpx;
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: space-between;
  transition: all 0.2s;
}

.history-record:active {
  transform: translateX(-8rpx);
}

.record-info {
  flex: 1;
}

.record-value {
  font-size: 40rpx;
  font-family: var(--font-family-number);
  font-weight: var(--font-weight-bold);
  color: var(--text-primary);
  display: flex;
  align-items: center;
}

.record-icon {
  font-size: 40rpx;
  margin-right: 12rpx;
}

.record-unit {
  font-size: 24rpx;
  color: var(--text-secondary);
  margin-left: 8rpx;
  font-weight: var(--font-weight-normal);
}

.record-meta {
  font-size: 24rpx;
  color: var(--text-secondary);
  margin-top: 8rpx;
}

.record-actions {
  display: flex;
  gap: 16rpx;
}

.record-action-button {
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: var(--bg-secondary);
  font-size: 28rpx;
}

.record-delete-button {
  background: var(--danger-bg);
  color: var(--danger-text);
}

.record-abnormal .record-value {
  color: var(--danger-color);
}
```

---

## 5. 血糖趋势页面(trend-glucose)

### 5.1 页面布局图

```
┌─────────────────────────────────┐
│ ← 血糖趋势                      │
├─────────────────────────────────┤
│  [全部▼]  [空腹]  [早餐后]...   │ ← 测量时段筛选
├─────────────────────────────────┤
│  [最近7天▼]                   │ ← 时间范围选择
├─────────────────────────────────┤
│    统计数据                     │
│  ┌───────────────────────────┐  │
│  │ 平均      最高      最低    │  │ ← 统计卡片
│  │ 6.8      10.5     5.2     │  │    横向排列
│  │ mmol/L   mmol/L   mmol/L   │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │        异常次数: 2         │  │
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│                                 │
│  ┌───────────────────────────┐  │
│  │                           │  │
│  │    血糖趋势图             │  │ ← 折线图区域
│  │                           │  │    大尺寸图表
│  │   ╱─╲                    │  │
│  │  ╱   ╲  ● (异常点)       │  │    橙色主线
│  │ ╱     ╲╱                │  │    红色异常点
│  │╱────────╲                │  │
│  │                           │  │
│  │ 01/26 01/27 01/28...     │  │
│  └───────────────────────────┘  │
│                                 │
├─────────────────────────────────┤
│    详细数据                     │
│  ┌───────────────────────────┐  │
│  │ 2026-01-31  空腹        │  │
│  │ 7.2 mmol/L  [异常]       │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ 2026-01-31  早餐后      │  │
│  │ 10.5 mmol/L [异常]      │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ 2026-01-30  空腹        │  │
│  │ 6.8 mmol/L             │  │
│  └───────────────────────────┘  │
│                                 │
├─────────────────────────────────┤
│   [首页] [记录] [我的]         │
└─────────────────────────────────┘
```

### 5.2 组件设计规范

#### 统计卡片组
```css
.statistics-group {
  background: var(--bg-primary);
  border-radius: var(--radius-lg);
  padding: 32rpx;
  margin-bottom: 24rpx;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16rpx;
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 24rpx 16rpx;
  background: var(--bg-secondary);
  border-radius: var(--radius-md);
}

.stat-label {
  font-size: 24rpx;
  color: var(--text-secondary);
  margin-bottom: 8rpx;
}

.stat-value {
  font-size: 48rpx;
  font-family: var(--font-family-number);
  font-weight: var(--font-weight-bold);
  color: var(--primary-color);
}

.stat-unit {
  font-size: 20rpx;
  color: var(--text-tertiary);
  margin-top: 4rpx;
}
```

#### 异常统计卡片
```css
.abnormal-stat-card {
  background: linear-gradient(135deg, #FF6B6B, #FF8787);
  border-radius: var(--radius-lg);
  padding: 32rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24rpx;
}

.abnormal-stat-icon {
  font-size: 64rpx;
}

.abnormal-stat-content {
  flex: 1;
  margin-left: 24rpx;
}

.abnormal-stat-label {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.9);
  margin-bottom: 8rpx;
}

.abnormal-stat-value {
  font-size: 48rpx;
  font-family: var(--font-family-number);
  font-weight: var(--font-weight-bold);
  color: #FFFFFF;
}
```

---

## 6. 血压趋势页面(trend-pressure)

### 6.1 页面布局图

```
┌─────────────────────────────────┐
│ ← 血压趋势                      │
├─────────────────────────────────┤
│  [最近7天▼]  [最近30天]       │ ← 时间范围选择
├─────────────────────────────────┤
│    统计数据                     │
│  ┌───────────────────────────┐  │
│  │ 平均收缩压  平均舒张压    │  │ ← 统计卡片
│  │  141 mmHg   91 mmHg     │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ 最高     最低     异常     │  │
│  │ 158/99  130/82   2次      │  │
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│                                 │
│  ┌───────────────────────────┐  │
│  │                           │  │
│  │    血压趋势图(双曲线)      │  │ ← 双曲线图
│  │                           │  │    橙色:收缩压
│  │  ╱──╲  ● (收缩压异常)     │  │    蓝色:舒张压
│  │ ╱    ╲╱                 │  │
│  │╱──────╲╱                 │  │
│  │  ╱──╲                    │  │    红色点:异常
│  │ ╱    ╲                   │  │
│  │╱──────╲                   │  │
│  │                           │  │
│  └───────────────────────────┘  │
│  ○ 橙色:收缩压  ● 蓝色:舒张压 │
│                                 │
├─────────────────────────────────┤
│    详细数据                     │
│  ┌───────────────────────────┐  │
│  │ 2026-01-31               │  │
│  │ 收缩压 145  舒张压 95    │  │
│  │ [异常] 心率 78           │  │
│  └───────────────────────────┘  │
│                                 │
├─────────────────────────────────┤
│   [首页] [记录] [我的]         │
└─────────────────────────────────┘
```

---

## 7. 个人中心页面(profile)

### 7.1 页面布局图

```
┌─────────────────────────────────┐
│ ← 我的                          │
├─────────────────────────────────┤
│                                 │
│  ┌───────────────────────────┐  │
│  │     👤                   │  │ ← 用户信息卡片
│  │                           │  │    大头像 + 姓名
│  │       张大爷              │  │
│  │       70岁  男           │  │
│  └───────────────────────────┘  │
│  [编辑个人信息]                  │
├─────────────────────────────────┤
│                                 │
│  ┌───────────────────────────┐  │
│  │ 📊 血糖趋势    [>]       │  │ ← 功能菜单
│  ├───────────────────────────┤  │
│  │ 💓 血压趋势    [>]       │  │
│  ├───────────────────────────┤  │
│  │ 📋 历史记录    [>]       │  │
│  ├───────────────────────────┤  │
│  │ ⚙️  健康信息    [>]       │  │
│  ├───────────────────────────┤  │
│  │ ❓ 关于我们     [>]       │  │
│  └───────────────────────────┘  │
│                                 │
├─────────────────────────────────┤
│    使用统计                     │
│  ┌───────────────────────────┐  │
│  │ 血糖记录: 156次           │  │
│  │ 血压记录: 148次           │  │
│  │ 使用天数: 45天            │  │
│  └───────────────────────────┘  │
│                                 │
├─────────────────────────────────┤
│    [退出登录]                   │ ← 危险按钮
│                                 │
├─────────────────────────────────┤
│   [首页] [记录] [我的]         │
└─────────────────────────────────┘
```

### 7.2 组件设计规范

#### 用户信息卡片
```css
.user-info-card {
  background: var(--bg-primary);
  border-radius: var(--radius-lg);
  padding: 48rpx 32rpx;
  margin-bottom: 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  box-shadow: var(--shadow-md);
}

.user-avatar {
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  background: var(--secondary-color);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 80rpx;
  margin-bottom: 24rpx;
  border: 4rpx solid var(--primary-color);
}

.user-name {
  font-size: 40rpx;
  font-weight: var(--font-weight-bold);
  color: var(--text-primary);
  margin-bottom: 8rpx;
}

.user-details {
  font-size: 28rpx;
  color: var(--text-secondary);
}

.edit-profile-button {
  margin-top: 24rpx;
  padding: 16rpx 48rpx;
  background: var(--secondary-color);
  color: var(--primary-color);
  border-radius: 100rpx;
  font-size: 28rpx;
  font-weight: var(--font-weight-medium);
}
```

#### 功能菜单
```css
.menu-list {
  background: var(--bg-primary);
  border-radius: var(--radius-lg);
  overflow: hidden;
  margin-bottom: 32rpx;
}

.menu-item {
  display: flex;
  align-items: center;
  padding: 32rpx;
  border-bottom: 1rpx solid var(--border-primary);
  transition: background 0.2s;
}

.menu-item:last-child {
  border-bottom: none;
}

.menu-item:active {
  background: var(--bg-secondary);
}

.menu-icon {
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--secondary-color);
  border-radius: var(--radius-md);
  font-size: 32rpx;
  margin-right: 24rpx;
}

.menu-text {
  flex: 1;
  font-size: 32rpx;
  color: var(--text-primary);
  font-weight: var(--font-weight-medium);
}

.menu-arrow {
  color: var(--text-tertiary);
  font-size: 32rpx;
}
```

---

**文档结束**
